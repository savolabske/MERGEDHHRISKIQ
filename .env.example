# Mapbox public access token (client-side). Create at https://account.mapbox.com/access-tokens/
# Required for the Maps view. On Netlify: Site settings → Environment variables → add this key.
VITE_MAPBOX_ACCESS_TOKEN=
